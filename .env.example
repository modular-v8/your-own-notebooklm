# Copy to .env and fill in whichever provider(s) you'll use.
# The Claude Agent SDK provider needs neither -- it authenticates via the
# local `claude` CLI's existing subscription login (`claude login`).

ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
